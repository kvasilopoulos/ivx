% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ivx-episodic.R
\name{ivx_episodic}
\alias{ivx_episodic}
\alias{print.ivx_episodic}
\title{Subsample IVX Tests for Episodic Predictability}
\usage{
ivx_episodic(
  object,
  scheme = c("rolling", "forward", "backward"),
  window = 0.2,
  robust = FALSE,
  B = 999,
  type = c("frwb", "rwb"),
  ar_max = 5,
  dist = c("rademacher", "normal"),
  seed = NULL,
  cores = 1L
)

\method{print}{ivx_episodic}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{object}{an object of class "ivx" fitted with \code{horizon = 1}.}

\item{scheme}{\code{"rolling"} windows of fixed width, \code{"forward"} recursive
windows starting at the first observation, or \code{"backward"} recursive windows
ending at the last observation.}

\item{window}{fraction of the sample: the window width for \code{"rolling"}, the
warm-in fraction \eqn{\tau_L} for \code{"forward"}, and the latest start
\eqn{\tau_U} for \code{"backward"}.}

\item{robust}{logical; use Eicker-White standard errors in the subsample
statistics.}

\item{B}{number of bootstrap replications.}

\item{type}{bootstrap scheme, see \code{\link[=ivx_boot]{ivx_boot()}}. The default fixed regressor
wild bootstrap is the scheme used by Demetrescu et al. (2022).}

\item{ar_max}{maximum lag order of the (vector) autoregression fitted to the
regressors by the RWB scheme; the order is selected by BIC (Remark 24).}

\item{dist}{distribution of the wild multipliers.}

\item{seed}{optional integer seed. With \code{cores > 1} the L'Ecuyer-CMRG
streams of the \pkg{parallel} package are used, so results are reproducible
for a given \code{seed} and \code{cores} but differ from the serial run.}

\item{cores}{number of CPU cores. Uses forking on Unix and a PSOCK cluster on
Windows (the package must be installed for the workers to load it).}

\item{x}{an object of class "ivx_episodic".}

\item{digits}{minimal number of significant digits.}

\item{...}{unused.}
}
\value{
an object of class "ivx_episodic": the observed sequence
(\code{sequence}, one row per window with its start/end index and statistics),
the sup statistics (\code{statistic}) and their bootstrap p-values (\code{p.value}),
plus the bootstrap draws (\code{boot}).
}
\description{
Tests for "pockets" of predictability using the suprema of sequences of
subsample IVX statistics (Demetrescu et al., 2023, Section 3.2; Demetrescu
et al., 2022). For each window the IVX statistic is computed from the
window's observations with the full-sample instrument (eqs 15-17); the test
statistics are the maximum (right-tailed), minimum (left-tailed) and maximum
squared (two-sided) t-ratio over the sequence for a single predictor, and
the maximum Wald statistic for several predictors (Remark 11). P-values are
obtained by wild bootstrap (Algorithms 1-2), which the paper shows to be
asymptotically valid for these sup-functionals.
}
\examples{
mod <- ivx(Ret ~ DP, data = kms)
ivx_episodic(mod, scheme = "rolling", window = 0.2, B = 99, seed = 1)
}
\references{
Demetrescu, M., Georgiev, I., Rodrigues, P. M. M., & Taylor, A. M. R.
(2022). Testing for episodic predictability in stock returns. Journal of
Econometrics, 227(1), 85-113.

Demetrescu, M., Georgiev, I., Rodrigues, P. M. M., & Taylor, A. M. R.
(2023). Extensions to IVX methods of inference for return predictability.
Journal of Econometrics, 237(2), 105271.
}
