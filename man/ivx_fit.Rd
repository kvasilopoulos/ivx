% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ivx.R
\name{ivx_fit}
\alias{ivx_fit}
\alias{ivx_wfit}
\title{Fitter Functions for IVX Models}
\usage{
ivx_fit(
  y,
  x,
  horizon = 1,
  offset = NULL,
  beta = 0.95,
  cz = 1,
  bandwidth = NULL,
  robust = FALSE,
  lag_y = FALSE,
  ...
)

ivx_wfit(
  y,
  x,
  w,
  horizon = 1,
  offset = NULL,
  beta = 0.95,
  cz = 1,
  bandwidth = NULL,
  robust = FALSE,
  lag_y = FALSE,
  ...
)
}
\arguments{
\item{y}{vector of observations of length \code{n}, or a matrix with
    \code{n} rows.}

\item{x}{design matrix of dimension \code{n * p}.}

\item{horizon}{is the horizon (default horizon = 1 corresponds to a
short-horizon regression). For \code{horizon > 1} the estimator and modified
Wald statistic are those of Kostakis et al. (2023), eqs (15) and (23):
K-period sums of the response and of the lagged predictors, a single-lag
instrument in the signal matrix and the K-period sum of the instrument in
the covariance.}

\item{offset}{(numeric of length \code{n}).  This can be used to
    specify an \emph{a priori} known component to be included in the
    linear predictor during fitting.}

\item{beta, cz}{tuning parameters of the IVX instrument
\eqn{z_t = \sum_{j=0}^{t-1} (1 - c_z/n^\beta)^j \Delta x_{t-j}}. Defaults
(\code{beta = 0.95}, \code{cz = 1}) follow Kostakis et al. (2015).}

\item{bandwidth}{Newey-West bandwidth for the long-run covariance estimate.
The default \code{NULL} uses \eqn{n^{1/3}} as in Kostakis et al. (2015).}

\item{robust}{logical. If \code{TRUE} the Eicker-White (heteroskedasticity-robust)
form of the IVX covariance matrix is used (Demetrescu et al., 2023). Only
available for \code{horizon = 1}.}

\item{lag_y}{logical. If \code{TRUE} the regression is augmented with the lagged
dependent variable (column \code{y_lag}), instrumented by itself, as in
Demetrescu (2014): this can raise the local power of the IVX test when the
predictors are highly persistent and endogenous, at no cost otherwise. The
joint Wald statistic still tests only the predictors. Only for \code{horizon = 1}.}

\item{...}{currently disregarded.}

\item{w}{vector of weights (length \code{n}) to be used in the fitting
    process for the \code{wfit} functions.  Weighted least squares is
    used with weights \code{w}, i.e., \code{sum(w * e^2)} is minimized.}
}
\description{
Basic function called by \code{ivx} to fit predictive models.
These should only be used directly by experienced users.
}
\examples{
ivx_fit(monthly$Ret, as.matrix(monthly$LTY))
}
