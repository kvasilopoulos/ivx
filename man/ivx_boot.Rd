% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ivx-boot.R
\name{ivx_boot}
\alias{ivx_boot}
\alias{print.ivx_boot}
\title{Wild Bootstrap Inference for IVX Models}
\usage{
ivx_boot(
  object,
  B = 999,
  type = c("rwb", "frwb"),
  ar_max = 5,
  dist = c("rademacher", "normal"),
  seed = NULL
)

\method{print}{ivx_boot}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{object}{an object of class "ivx" (not "ivx_ar"), fitted without weights.}

\item{B}{number of bootstrap replications.}

\item{type}{bootstrap scheme: \code{"rwb"} (residual wild bootstrap, recommended
for strongly persistent regressors) or \code{"frwb"} (fixed regressor wild bootstrap).}

\item{ar_max}{maximum AR order (selected by AIC) for the regressor
autoregression used by the RWB scheme.}

\item{dist}{distribution of the wild multipliers.}

\item{seed}{optional integer passed to \code{\link[=set.seed]{set.seed()}}.}

\item{x}{an object of class "ivx_boot".}

\item{digits}{minimal number of significant digits.}

\item{...}{further arguments passed to \code{\link[=printCoefmat]{printCoefmat()}}.}
}
\value{
an object of class "ivx_boot": a list with the observed statistics,
the bootstrap distributions (\code{boot}), and bootstrap p-values (\code{p.value}).
\code{p.value$tstat} has one column per alternative: two-sided, \code{beta < 0} and
\code{beta > 0}.
}
\description{
Computes bootstrap p-values for the IVX Wald and t statistics using the
residual wild bootstrap (RWB) or the fixed regressor wild bootstrap (FRWB) of
Demetrescu et al. (2023). The null hypothesis of no predictability is imposed
on the bootstrap samples. RWB rebuilds the regressor from an AR fit and its
residuals (multiplied by the same wild multiplier as the predictive-regression
residuals, so the innovation correlation is preserved); FRWB keeps the
regressors and instruments fixed and only resamples the response.
}
\examples{
mod <- ivx(Ret ~ DP + TBL, data = kms)
ivx_boot(mod, B = 199, seed = 1)
}
\references{
Demetrescu, M., Georgiev, I., Rodrigues, P. M. M., & Taylor, A. M. R.
(2023). Extensions to IVX methods of inference for return predictability.
Journal of Econometrics, 237(2), 105271.
}
