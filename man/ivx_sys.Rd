% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ivx-sys.R
\name{ivx_sys}
\alias{ivx_sys}
\alias{print.ivx_sys}
\alias{summary.ivx_sys}
\title{Fitting Systems of IVX Predictive Regressions}
\usage{
ivx_sys(
  formula,
  data,
  horizon,
  na.action,
  contrasts = NULL,
  model = TRUE,
  x = FALSE,
  y = FALSE,
  beta = 0.95,
  cz = 1,
  bandwidth = NULL,
  ...
)

\method{print}{ivx_sys}(x, digits = max(3L, getOption("digits") - 3L), ...)

\method{summary}{ivx_sys}(object, ...)
}
\arguments{
\item{formula}{a formula whose left-hand side is a matrix, e.g.
\code{cbind(y1, y2) ~ x1 + x2}.}

\item{data}{n optional data frame, list or environment (or object coercible by
\code{\link[base:as.data.frame]{as.data.frame}} to a data frame) containing
the variables in the model. If not found in data, the variables are taken
from environment(formula), typically the environment from which lm is called.}

\item{horizon}{is the horizon (default horizon = 1 corresponds to a
short-horizon regression). For \code{horizon > 1} the estimator and modified
Wald statistic are those of Kostakis et al. (2023), eqs (15) and (23):
K-period sums of the response and of the lagged predictors, a single-lag
instrument in the signal matrix and the K-period sum of the instrument in
the covariance.}

\item{na.action}{a function which indicates what should happen when the data
contain NAs. The default is set by the na.action setting of \code{\link[base:options]{options}},
and is \code{\link[stats:na.fail]{na.fail}} if that is unset. The ‘factory-fresh’
default is \code{\link[stats:na.fail]{na.omit}}. Another possible value is \code{NULL},
no action. Value \code{\link[stats:na.fail]{na.exclude}} can be useful.}

\item{contrasts}{an optional list. See the \code{contrasts.arg} of
\code{\link[stats:model.matrix]{model.matrix.default}}.}

\item{model}{logical. If \code{TRUE} the model.frame of the fit is returned.}

\item{x}{an object of class "ivx_sys".}

\item{y}{logical. If \code{TRUE} the response of the fit is returned.}

\item{beta, cz}{tuning parameters of the IVX instrument
\eqn{z_t = \sum_{j=0}^{t-1} (1 - c_z/n^eta)^j \Delta x_{t-j}}. Defaults
(\code{beta = 0.95}, \code{cz = 1}) follow Kostakis et al. (2015).}

\item{bandwidth}{Newey-West bandwidth for the long-run covariance estimate.
The default \code{NULL} uses \eqn{n^{1/3}} as in Kostakis et al. (2015).}

\item{...}{additional arguments to be passed to the low level regression fitting
functions (see \link[stats:lm]{lm}).}

\item{digits}{the number of significant digits to use when printing.}

\item{object}{an object of class "ivx_sys".}
}
\value{
an object of class "ivx_sys" with the coefficient matrix
\code{coefficients} (responses in rows, predictors in columns), matching \code{se},
\code{tstat} and \code{Wald_Ind} matrices, the joint Wald statistic \code{Wald_Joint}
(\eqn{\chi^2(mr)}), the per-equation Wald statistics \code{Wald_Eq}
(\eqn{\chi^2(r)}), and \code{vcov} of \code{vec(coefficients)} (column-major, names
\code{response:predictor}).
}
\description{
\code{ivx_sys} estimates a system of predictive regressions
\eqn{y_t = \mu + A x_{t-1} + \varepsilon_t} with an \eqn{m}-vector response
and \eqn{r} predictors of arbitrary persistence by IVX, and tests linear
restrictions on \eqn{A} with the IVX-Wald statistic of Kostakis,
Magdalinos and Stamatogiannis (2023), eqs (15) and (23), whose covariance
has the Kronecker form \eqn{(Z'X)^{-1} \otimes I_m} around
\eqn{Z(K)'Z(K) \otimes \hat\Sigma - n \bar z \bar z' \otimes \hat\Sigma_{FM}}.
Long horizons are handled as in \code{\link[=ivx]{ivx()}}. For \eqn{m = 1} the results
coincide with \code{ivx()}.
}
\examples{
ivx_sys(cbind(Ret, DE) ~ DP + TBL, data = kms)

summary(ivx_sys(cbind(Ret, DE) ~ DP + TBL, data = kms, horizon = 4))
}
\references{
Kostakis, A., Magdalinos, T., & Stamatogiannis, M. P. (2023).
Taking stock of long-horizon predictability tests: Are factor returns
predictable? Journal of Econometrics, 237(2), 105380.
}
