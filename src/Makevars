
# Use default C++ standard (no explicit specification)
# This avoids CRAN NOTE about specifying C++11 unless essential

# PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS =  $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) #$(SHLIB_OPENMP_CFLAGS)
